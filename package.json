{
  "name": "chainlink",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "hardhat:build": "bun run --filter @mono/contracts build",
    "hardhat:test": "bun run --filter @mono/contracts test",
    "hardhat:test:sol": "bun run --filter @mono/contracts test:sol"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
